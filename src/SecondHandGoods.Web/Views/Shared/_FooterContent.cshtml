@* Featured listings and paid ads are separate full-width sections above the main site footer (avoids layout overlap). *@
@await Html.PartialAsync("_FeaturedListingsSection")
@await Html.PartialAsync("_PaidAdsSection")
